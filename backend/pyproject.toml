[project]
name = "bigpopa-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic",
  "pydantic-settings",
  "pytest",
  "pandas",
  "openpyxl",
  "pyarrow",
]

[tool.pytest.ini_options]
pythonpath = ["."]
