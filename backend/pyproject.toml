[project]
name = "bigpopa-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic",
  "pytest",
  "httpx"
]

[tool.pytest.ini_options]
pythonpath = ["app"]
